package com.alten.opencvocr.repository;

import com.alten.opencvocr.entity.InputImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InputImageRepository extends JpaRepository<InputImage,Long> {
}
